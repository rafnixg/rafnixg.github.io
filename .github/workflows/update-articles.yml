name: Update Articles & Resume JSON

on:
  schedule:
    - cron: '0 0 1 * *' # run monthly on day 1
  workflow_dispatch: {}

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      - name: Install dependencies
        run: npm ci
      - name: Fetch articles
        run: node build/fetch-articles.js
      - name: Fetch resume
        run: node build/fetch-resume.js
      - name: Commit updated data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/articles.json data/resume.json || true
          git diff --quiet && echo "No changes" || (git commit -m "chore: update articles.json and resume.json" && git push)
